description: >
  Alt-Repeat Behavior
  Repeat the previous keycode unless when specified

compatible: "zmk,behavior-alt-repeat"

include: zero_param.yaml

properties:
  usage-pages:
    type: array
    required: true
